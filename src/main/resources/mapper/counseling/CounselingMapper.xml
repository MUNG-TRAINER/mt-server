<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mungtrainer.mtserver.counseling.dao.CounselingDao">

    <insert id="insertCounseling" parameterType="com.mungtrainer.mtserver.counseling.entity.Counseling" useGeneratedKeys="true" keyProperty="counselingId">
        INSERT INTO counseling
        (dog_id, phone, content, is_completed, created_by, updated_by)
        VALUES
        (#{dogId}, #{phone}, #{content}, #{isCompleted}, #{createdBy}, #{updatedBy});
    </insert>


    <update id="cancelCounseling" parameterType="long">
        UPDATE counseling
        SET is_deleted = 1,
        deleted_at = NOW(),
        updated_at = NOW()
        WHERE counseling_id = #{counselingId}
        AND is_deleted = 0;
    </update>

    <!-- 상담 조회 -->
    <select id="findById" resultType="com.mungtrainer.mtserver.counseling.entity.Counseling" parameterType="long">
        SELECT *
        FROM counseling
        WHERE counseling_id = #{counselingId}
        AND is_deleted = 0
    </select>

</mapper>