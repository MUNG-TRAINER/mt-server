<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mungtrainer.mtserver.counseling.dao.TrainerUserDao">
    <!-- 훈련사가 관리하는 회원 목록 조회 -->
    <select id="findUsersByTrainerId"
            resultType="com.mungtrainer.mtserver.counseling.dto.response.TrainerUserListResponseDto">
        SELECT
        u.user_id AS userId,
        u.name AS name,
        u.phone AS phone,
        u.email AS email
        FROM trainer_user tu
        JOIN user u ON tu.user_id = u.user_id
        WHERE tu.trainer_id = #{trainerId}
        AND tu.is_deleted = 0
        AND u.is_deleted = 0
        ORDER BY u.created_at DESC
    </select>
</mapper>